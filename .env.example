# Chiave Anthropic — server-side ONLY, mai nel bundle client.
# NON serve per i prezzi (arrivano da fonti gratuite). Serve solo alle funzioni
# a richiesta: rating analisti, analisi, chat, sottostanti, lettura PDF delle call.
ANTHROPIC_API_KEY=

# Modello di partenza, usato finché non se ne sceglie uno dal menù a tendina in
# dashboard (quella scelta, una volta fatta, ha sempre la priorità su questa
# variabile). Se lasci vuoto, il codice usa claude-opus-5. Vedi README "Costi".
ANTHROPIC_MODEL=claude-sonnet-5

# Store dati: JSON versionati in questo stesso repo, letti/scritti via GitHub API
# in produzione (il filesystem delle funzioni serverless Vercel è effimero).
# Token fine-grained con permesso "Contents: Read and write" SOLO su questo repo.
GITHUB_TOKEN=
GITHUB_REPO=robertofolco82/finance
GITHUB_BRANCH=main

# "fs" | "github" — normalmente non serve impostarla: fs in locale (VERCEL non
# è definita), github in automatico su Vercel. Utile solo per forzare un backend
# durante debug.
# DATA_BACKEND=
